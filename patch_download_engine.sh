sed -i 's/val connection = URL(url).openConnection() as HttpURLConnection/var connection: HttpURLConnection? = null\n                var finalUrl = url\n                var redirectCount = 0\n                while (redirectCount < 10) {\n                    connection = URL(finalUrl).openConnection() as HttpURLConnection\n                    connection.instanceFollowRedirects = false/g' app/src/main/java/com/example/engine/DownloadEngine.kt
